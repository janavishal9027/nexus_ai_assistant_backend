"""Layered memory & knowledge (Part D).

Phase 1 (foundation): working (in-session ring buffer) + episodic (durable
per-user Q&A log with real semantic embeddings). Later phases add semantic
memory (skills/prefs/lessons), the Reflector, lifecycle, project brain, and the
content knowledge graph. See docs/semantic-embedding/ conventions.
"""
