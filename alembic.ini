# Alembic configuration for the agent-orchestration migrations.
# Run from the backend/ directory:  alembic upgrade head
[alembic]
script_location = migrations
prepend_sys_path = .
# The database URL is resolved from Settings inside migrations/env.py, so it is
# intentionally left blank here.
sqlalchemy.url =
